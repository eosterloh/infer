[pytest]
testpaths = tests
pythonpath = .
markers =
    integration: needs local model weights
    spark: needs DGX Spark CUDA + ~/models/Llama-3.2-1B-Instruct
filterwarnings =
    ignore::DeprecationWarning

[pytest]
testpaths = tests
pythonpath = .
markers =
    integration: needs local model weights
    spark: needs DGX Spark CUDA and an explicitly configured model checkpoint
filterwarnings =
    ignore::DeprecationWarning
